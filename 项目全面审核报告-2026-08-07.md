# 识海 Noetide 项目全面审核报告

- 审核日期：2026-08-07
- 审核基线：branch `main` @ `9e3875d`（工作树干净）
- 审核方式：六路并行独立审核（文档治理 / 核心源码 / 产品层与安全 / 安全隐私依赖 / 测试验证体系 / 实际重跑验证），静态审查 + 全部可执行声明实际复跑
- 验证环境：Windows + Git Bash，CPython 3.12.8
- 说明：`Review-report/` 下的既有第三方报告按本次审核要求未读取，本报告全部结论来自独立审查与实测。

---

## 1. 执行摘要

**总体评级：治理与验证体系优秀且经实测可信；产品层安全存在 1 个 P0 与若干 P1，需在下一发布前修复。**

项目在两个维度上表现突出：

1. **文档治理链路扎实**：抽查切片（Y2-S4、B3）从 Decision → SPEC → Traceability → ADR → suite 物化 → Implementation Plan → 结果 → Gate Review → recovery tag 十一环全闭合；PRD v04/v05/v06 三代哈希链实测未被篡改。
2. **验证声明全部复跑成立**：PROJECT_STATE 中 7 项可执行声明（全量回归 485 OK 0 skipped、产品测试 5/5、26 个 suite validator、2 个 baseline validator、PRDv06 哈希绑定、rev_010 smoke、tag/dist 哈希）于 2026-08-07 全部实际重跑，**零不一致**。

最严重的问题集中在**产品形态的最后一公里**：

- **P0**:`product_server.py` 默认零鉴权 + `Access-Control-Allow-Origin: *`，任意恶意网页可经用户浏览器读取全部记忆数据、导入本机文件、并将记忆原文外发到攻击者服务器。
- **P1 簇**：产品云端模型通路完全绕开已验证的 CloudGate 授权/红线/预览门；`--host 0.0.0.0` 无校验即局域网无认证暴露；备份加密无 KDF 且 UI 无风险提示；状态文档滞后（`product-complete-rp-20260803` tag 已打但无任何文档记录）；ChangeSet 合同在 store 层无强制且多个模块实质绕过；revision 分配存在五种互不兼容方案，跨模块共库时真实可碰撞。

---

## 2. 实际重跑验证结果（2026-08-07 实测）

| 项目 | 命令（摘要） | exit code | 结果 | 与 PROJECT_STATE 声明 |
|---|---|---|---|---|
| 全量 configured-adapter 回归 | `PYTHONPATH=src` + 21 个 adapter 环境变量 + `python -m unittest discover -s tests -t .` | 0（连跑 2 次） | **485 passed / 0 skipped / 0 failed**，128s | ✓ 一致 |
| 产品测试 | `python -m unittest tests.semantic.test_product_app -v` | 0 | 5/5 OK | ✓ 一致 |
| 26 个 suite validator | 逐个 `python tools/validate_*_suite.py` | 26/26 全部 0 | 全部 PASSED | ✓ 一致 |
| Product baseline 校验 | `powershell -File tools/validate_product_baseline.ps1` | 0 | PASSED | ✓ 一致 |
| SPEC baseline 校验 | `powershell -File tools/validate_spec_baseline.ps1` | 0 | PASSED（185 引用、227 文件隐私扫描、700 文件围栏全 PASS） | ✓ 一致 |
| PRDv06 哈希 | 独立复算 canonical LF SHA-256 | — | `4513B268…CAD8`，逐字符一致 | ✓ 一致 |
| start.py smoke | `python start.py`（未用 `--clean`） | 0 | `revision rev_010, pragmas OK` | ✓ 一致 |
| Git tags | `git tag -l` / `git rev-parse` | — | `v0.2.0-beta`→`08095cc4`；a2–a6/b2–b6/c2–c6/y2s1–y2s5/d2 recovery tags 全部存在 | ✓ 一致 |
| dist 哈希 | `sha256sum dist/Noetide-beta-v0.3.0-win64.zip` | 0 | `55c26e39…b80f2`，与 SUMS 文件一致 | ✓ 一致 |

附注：直接 `pytest tests/semantic` 会因 src 布局无 conftest 而收集失败——官方回归命令是 unittest discover + adapter 环境变量，建议在 `tests/README.md` 补记以免误判。

---

## 3. 分领域审核结论

### 3.1 文档与治理一致性

**合规确认**：
- PRD v06/v05/v04 三代哈希链实测一致，历史基线未被篡改（v04 哈希声明出处为 `tools/validate_product_baseline.ps1:44`）。
- PROJECT_STATE 抽查 12 条声明（tag、dist 哈希、结果文件、Gate Review）全部属实。
- Y2-S4、B3 两切片门禁链十一环全闭合；矩阵引用的结果文件无断链。
- OPEN_QUESTIONS 与项目状态一致（DQ-013 已 decided，blocking=0）。

**发现**：
- **P1-文1** `product-complete-rp-20260803` annotated tag 已存在且指向 HEAD，但 `docs/PROJECT_STATE.md:15,213` 与 `docs/process/CURRENT_HANDOFF.md` 仍记"待发布"，全仓库无任何文档记录该 tag，`docs/releases/` 无对应 Recovery Record——违反 `docs/process/README.md:87` 的门禁合同与 AGENTS.md 的收尾规则。
- **P1-文2** PROJECT_STATE §3 缺 Y2-S5 条目（止于第 73 条 Y2-S4），与 Y2-S1..S4 体例断裂。
- **P1-文3** `docs/process/README.md:11,157-171` 的"当前快照"仍停留在 v0.5 / Answer Safety 切片，与基线 v0.6、产品已完成的事实直接矛盾；该文件是 AGENTS.md 强制恢复顺序第 5 步，会误导恢复者。
- P2:PROJECT_STATE §3 编号 6 跳 14 无说明；OPEN_QUESTIONS §1 小节顺序乱（1.1,1.2,1.3,1.6,1.7,1.5,1.4）；基线文档未自含 v04 哈希（靠脚本兜底）。

### 3.2 核心源码质量（src/noetide_micro 核心层，约 15.7k 行总量）

**发现**：
- **P1-核1 revision 分配无统一仲裁，跨模块共库可碰撞/崩溃**:`changesets.py:73-87,156,181` 硬编码 `rev_011/012`;`episodes.py:78,114` 硬编码 `rev_021/022`;`entity_merge.py:256-261`、`commitments.py:248-253` 用 `_next_revision()` 自增；`c1.py:31` 用 `rev_c1_*`;`hypotheses.py:28-29` 用 `rev_{id}_{N}`。任一模块先占用 `rev_012`,`ChangeSetService.revert` 即抛未捕获 `IntegrityError`;`current_revision()` 为 `rev_c1_*` 时 `entity_merge._next_revision()` 直接 `RuntimeError`。该路径在 `runtime.py` 组合下真实可达。
- **P1-核2 "所有语义写入走 ChangeSet 合同"无 store 层强制，且多模块实质绕过**:`scenarios.py:76,133,154` 直接写 canonical 对象，无 revision/changeset/receipt/事务；`hypotheses.py:112-116` 有 revision 无 changeset 账本；`product.py:526-544` `confirm_candidate` 5 条写不在事务内且 changeset 记录 `base_revision == published`（语义错误）。`app_shell.py:103-114` 的静态扫描只管 presentation 模块自身。
- **P1-核3 Web 层并发隐患**:`runtime.py:71` `check_same_thread=False` + `local_web.py:135` `ThreadingHTTPServer`，共享单连接无锁；并发 POST 时第二个 `BEGIN IMMEDIATE` 抛 `OperationalError` 未被处理器捕获（`local_web.py:315` 只捕 `KeyError/RuntimeError/ValueError`)。raw 文件备份（`pack_backup.py:175`）与写事务并发可拷出不一致镜像。
- **P1-核4 Web 备份密钥硬编码为公开常量**:`local_web.py:33,410` `key=BACKUP_KEY_HINT = "c5-synthetic-key-hint"`。
- P2（择要）:`access_policy.py:124,93` 对畸形时间输入抛异常而非 fail-closed deny;`pack_backup.py:149`、`portability.py:103,105` 缺键时 `KeyError` 逃逸；时间戳字典序比较混用时区偏移（`summaries.py:99`、`current_state.py:27-29`);receipt id 实例级计数器跨实例撞主键（`current_state.py:118-120` 等）;`answers.py:228,234` `valid_time=='current'` 字符串比较恒 not_covered、`answers.py:309-312` freshness 取第一条 source;`disambiguation.py:53-57`、`bilingual.py:107-111` 原地改写 ledger 与 append-only 叙述冲突；`store.transaction()` 不可重入；`decision.py` 的更新 API 无持久化路径（死端）。

**否定结论（审过未见问题）**:SQL 注入（全部参数化）、TODO/FIXME 残留、schema 一致性、连接资源泄漏。

**优点**:Derived 不作证据多层落实（`hypotheses.py:43-44`、`store.py:774-801`、`answers.py:57-62`);fail-closed 主流实现扎实（access_policy、reconciliation、restore_backup)；读路径不伪装新鲜（stale→canonical_fallback)；事务原子性有故障注入验证。

### 3.3 产品层与安全（product_server / webui / MCP / 云模型 / 启动器）

**安全发现**：
- **P0-安1 默认无鉴权 + `ACAO: *`，任意网页可读写本机全部隐私数据**:`product_server.py:34`（所有响应通配 CORS)、`:58-66`（`remote_access=False` 默认时 `_authorized()` 直接 True）、`:180-186`(OPTIONS 预检放行）。后果链：恶意网页可 `GET /api/sources` 读走全部记忆；`POST /api/ingest/folder` 导入本机任意目录（限 .md/.txt/.json/.csv）后读出，构成任意本地文件读取；`POST /api/settings` 改 cloud endpoint 后 `POST /api/analyze` 将记忆原文外发攻击者服务器。此为静态推断，浏览器可达性受 PNA 策略影响，建议补最小 PoC。
- **P1-安1 `--host` 无回环校验**:`product_server.py:200-230`、`noetide_desktop.py:25` 可绑 `0.0.0.0`，叠加默认无鉴权即局域网全开；同仓库 `local_web.py:126-132` 却有强制回环校验，标准不一致。另 webui 的 `remoteHost/remotePort` 设置只存储从不生效（功能未闭环）。
- **P1-安2 产品云通路完全绕开已验证的 CloudGate**:`product.py:430-464` 直接 `urlopen` 用户 endpoint 全文发送 source;`cloud_model.py` 整套授权门/红线门/预览门在 `product.py` 零引用。且产品侧 source compartments 恒为 `["personal"]`(`product.py:248`)，红线门即使接入也不触发。`model_mode=local` 不限制 loopback(`product.py:435`)，可指向任意 http 内网地址（明文外发/轻 SSRF)；错误 `str(exc)` 原样回客户端。
- **P1-安3 备份加密无 KDF 且作为产品功能出货无用户提示**:`pack_backup.py:154-179` 自造 XOR 密钥流、内嵌明文 SHA-256（可作离线爆破 oracle)、无 MAC;`product.py:702-708` 经 `/api/backup` 直接暴露。模块与 ADR-0017 已诚实声明非生产级，但 webui 无任何风险提示。
- P2：令牌比较非恒定时间（`product_server.py:66`，应用 `hmac.compare_digest`);`settings.json` 明文存令牌/密钥且权限未收紧（`product.py:192-198`);`public_settings()` 明文下发 `api_token`(`product.py:200-211`)；请求体无大小上限、Content-Length 解析可致异常（`product_server.py:48-56`)；错误信息泄露内部路径（`product_server.py:148-149`);MCP/CloudGate 审计 ID 内存计数器重启撞主键（`mcp_runtime.py:229-230`、`cloud_model.py:186,200`);`import_pack` 的哈希校验自指，只防损坏不防伪造（`product.py:715-718`)。

**质量发现**:`product_server.py:8,19-23` 死代码；`folder_import.py:125-139` 显式条目一律拒绝的误导性死代码；`_refresh_projection()` 全表扫描 + O(n²) 候选检查（已知规模上限）。

**优点**:XSS 防护到位（webui 统一 `esc()`/`textContent`)；路径安全扎实（symlink escape 检查、`_safe_relative` 拒绝 `..`、verify_pack 拒绝未知文件、备份/恢复"目标存在即拒绝");`start.py --clean` 防误删设计良好；MCP 能力模型（工具白名单、红线 compartment、幂等键、字段级 redaction）设计认真；无 subprocess 调用面。

### 3.4 安全/隐私/依赖专项

**合规确认**：
- **stdlib-only 声明属实**:`pyproject.toml:12` `dependencies = []`;src 全量 import 扫描零第三方；`cloud_model.py:614-629` 内置 stdlib 自检。
- **零硬编码凭据**、零真实个人数据进入 git 跟踪文件（手机号/身份证/邮箱正则扫描仅命中哈希误报与合成占位）;tests/fixtures 27 个目录抽查全部显式标注 `synthetic: true`。
- dist/ 四个 zip 清单抽查无数据目录/个人文件；`.gitignore` 覆盖 `.env*`、`*.pem`、`*.db`、devdata 等。
- 网络出口已枚举且治理严格（local_web/MCP 强制回环、LocalHttpBackend 强制 loopback、CloudGate 默认拒绝+红线 fail-closed+全台账）——除 3.3 的 P0/P1 外。

**发现**:
- **P1-专1 SECURITY.md 与 v0.3.0 实际攻击面脱节**:`SECURITY.md:11` 仍称"真实数据导入、云服务、远程 Agent 不在安全承诺范围"，但云端模型、任意路径文件夹导入、remote_access、MCP 均已交付。
- P2:`.gitignore:47` 的 `/devdata/` 行尾混入 `\r` 导致规则实际失效（当前无 devdata 入库，潜在隐患，建议顺手修正）；CI actions 未 SHA 固定（`ci.yml:14-15,41-42`，供应链面小）;tmp/（未跟踪）内历史审计文件含真实邮箱，若外发 tmp 产物需注意。

### 3.5 测试与验证体系

**体系优点（经独立重算确认）**:
- 26 个 suite manifest × 27 个 validator 一一对应，fixture/oracle/scenario/protocol/contract/runner/manifest 七角色齐全，零孤儿。
- validator 是真校验而非查存在：逐文件重算 sha256、AST 解析 contract 装饰器确认场景覆盖、校验 result 哈希绑定与逐行 passed 明细。a6/b3/y2s5 三条哈希链独立重算全部真实。
- official runner 执行前验 artifact 哈希、socket 级断网、记录环境戳记、输出不可覆盖。
- contract 测试断言真业务语义（全字典比对 + forbidden_mutations 不可变断言）；抽查 adapter 均真实调用业务代码（除下述一处）。
- 26 个 suite、238 个必需场景全部 executed+passed，无挂空 not_executed。

**发现**：
- **P1-测1 B3-002 adapter"自问自答"**:`b3_testing_adapter.py:88-93` 无论 `propose()` 是否真拒绝非法候选，都无条件返回 oracle 期望的 failure 字典；forbidden_mutations 守卫不覆盖该路径。若业务层 preflight 退化，该场景仍判 passed。抽查中唯一一处实质性自欺风险。
- P2：哈希链防篡改不防伪造（validator 不复跑测试、无签名、git_commit 自声明）——对"防自欺"威胁模型够用，但应明确边界；Y2S1–S5 validator 成功消息误印 "no business test executed"(`validate_y2s5_suite.py:85` 等）；约 34 个 task/unit 级测试模块不受 manifest 哈希绑定保护，可被悄悄削弱；`tests/results/answer_safety/` 6 个孤儿结果文件；micro contract 缺 skip 守卫导致裸 pytest 22 个 ERROR 噪音。

---

## 4. 问题汇总与修复优先级

| 级别 | 编号 | 问题 | 位置 |
|---|---|---|---|
| P0 | 安1 | 默认零鉴权 + `ACAO: *`，任意网页可读写/外发本机隐私数据 | `product_server.py:34,58-66,180-186` |
| P1 | 安1 | `--host` 无回环校验，局域网可无认证暴露；remoteHost/Port 设置不生效 | `product_server.py:200-230`、`noetide_desktop.py:25` |
| P1 | 安2 | 产品云通路绕开 CloudGate 三道门；local 模式不限 loopback | `product.py:430-464,435,248` |
| P1 | 安3 | 备份加密无 KDF/无 MAC，UI 无风险提示 | `pack_backup.py:154-179`、`product.py:702-708` |
| P1 | 专1 | SECURITY.md 未覆盖 v0.3.0 实际功能面 | `SECURITY.md:11` |
| P1 | 核1 | revision 分配五种方案互不兼容，跨模块共库可碰撞崩溃 | `changesets.py`、`episodes.py`、`entity_merge.py`、`c1.py`、`hypotheses.py` |
| P1 | 核2 | ChangeSet 合同无 store 层强制，scenarios/product/hypotheses 实质绕过 | `scenarios.py:76,133,154`、`product.py:526-544`、`hypotheses.py:112-116` |
| P1 | 核3 | ThreadingHTTPServer + 共享单连接无锁，并发写崩溃/交错 | `runtime.py:71`、`local_web.py:135,315` |
| P1 | 核4 | Web 备份密钥硬编码公开常量 | `local_web.py:33,410` |
| P1 | 测1 | B3-002 adapter 自问自答，可掩盖业务回归 | `b3_testing_adapter.py:88-93` |
| P1 | 文1 | product-complete recovery tag 已发布但无文档记录、缺 Recovery Record | `PROJECT_STATE.md:15,213`、`CURRENT_HANDOFF.md` |
| P1 | 文2 | PROJECT_STATE §3 缺 Y2-S5 条目 | `PROJECT_STATE.md:98` |
| P1 | 文3 | process README"当前快照"严重过期（仍 v0.5 / Answer Safety) | `docs/process/README.md:11,157-171` |
| P2 | — | 令牌非恒定时间比较；密钥明文落盘；fail-closed 缺口（access_policy/verify)；时间戳跨时区字典序比较；ledger 原地改写；receipt id 跨实例碰撞；34 个测试模块未绑定；Y2S validator 消息误导；`.gitignore` `\r` 失效；CI actions 未固定 SHA；孤儿结果文件等（详见 §3) | 各处 |

**建议修复顺序**:

1. **P0-安1**（下一发布前必须）：去掉 `ACAO: *`、校验 `Host`/`Origin` 拒绝非 loopback 来源、loopback 默认也要求配对令牌。
2. **P1-安1 → 安2 → 安3**：绑定非回环且无 token 时拒绝启动；产品云通路接入 CloudGate 并落实红线 compartment 标注；备份加密加 `hashlib.pbkdf2_hmac` 派生 + UI 风险明示。
3. **P1-核1/核2/核3**（产品化闭合必须）:`SemanticStore` 层加统一 `publish_changeset()` 闸门（集中分配 revision、强制 changeset+receipt+事务）;Web 数据库访问加锁/串行化。
4. **P1-测1**：修 B3-002 adapter 返回真实业务判定。
5. **P1-文1/文2/文3 + 专1**：纯文档收尾，成本极低，建议立即完成。

---

## 5. 审核局限性（如实声明）

- 未联网核验：GitHub Actions run 状态、GitHub Release 远端附件 digest 未复核（历史记录显示发布时已核）。
- P0-安1 的浏览器可达性为静态推断，未实际起服务做 PoC；Chrome Private Network Access 策略可能部分缓解公网页面→loopback 路径，但 http 页面不受 mixed-content 限制，不构成服务端防线。
- 源码审核为精读关键模块 + 抽样，非逐行全覆盖；测试体系可信度结论基于三条哈希链独立重算与 adapter 抽查。
- `Review-report/` 既有第三方报告按用户要求未读取、未参考。

---

## 6. 总体评价

识海 Noetide 在**治理纪律、验证体系可信度、依赖与隐私卫生**三个维度上显著高于同规模项目：门禁链路十一环闭合且机器化校验、哈希绑定经独立重算确认真实、全部测试声明于审核当日复跑零不一致、stdlib-only 与合成数据纪律完全属实。这些不是纸面合规，而是可独立复现的工程事实。

与之相对，**产品化的最后一公里尚未闭合**:Web 服务的攻击面防护（P0）、已验证安全门禁向产品通路的传导（CloudGate 旁路）、ChangeSet 合同的系统级强制、以及状态文档的收尾同步，是当前与"完整产品"自我定位之间的主要差距。历史切片级验证全部可信，但切片在统一产品库中的组合安全性（revision 碰撞、并发写、合同绕过）是下一阶段必须正面解决的问题。

**结论：作为逐切片验证的 beta，当前状态可信且可发布；作为承载真实个人数据的统一产品，需先修复 P0-安1 及 P1 安全簇，再补核1/核2/核3 的系统级闭合。**
