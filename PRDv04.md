# 识海 Noetide 产品需求文档

> PRD v0.4 · Personal Context & Growth Engine

| 文档属性 | 内容 |
|---|---|
| 文档状态 | Draft for Review |
| 版本 | 0.4 |
| 日期 | 2026-07-13 |
| 产品中文名 | 识海 |
| 产品英文工作名 | Noetide |
| 核心智能角色 | 识灵 Shiling / Noetide Curator |
| 产品阶段 | 产品定义与 MVP 收敛阶段 |
| 数据策略 | Local-first、User-owned、Portable、Correctable |
| 文档边界 | 本文定义产品行为、用户价值、边界、阶段与验收，不锁定具体技术实现 |

> 隐私说明：本文不引用任何真实个人档案。所有人物、经历、关系、金额、组织和场景均为虚构测试案例，仅用于说明产品需求。

---

## 目录

1. 执行摘要
2. 多模型审查建议评分与采纳决策
3. 产品愿景与定位
4. 问题定义
5. 目标用户与 Jobs to Be Done
6. 产品原则与语义宪法
7. 产品概念与系统边界
8. 核心语义对象模型
9. 时间、证据与认知状态
10. 反应式一致性系统
11. ChangeSet 变更协议
12. 冲突、纠正与撤销
13. 实体消歧与关系演化
14. 识灵设计
15. 审查预算与低打扰机制
16. 记忆生命周期与长期规模
17. 隐私、权限与第三方数据
18. 核心用户体验
19. MCP、Agent 与输入层边界
20. 功能需求
21. 非功能需求与服务等级
22. 评测、自证与模型升级
23. 指标体系
24. MVP 与路线图
25. 风险、降级与非目标
26. 虚构端到端验收案例
27. 开放问题与后续 SPEC

---

# 1. 执行摘要

识海不是一个等待用户手工维护的知识库，也不是给某个大模型附加的聊天记忆。识海是一个由用户长期拥有、可纠正、可追溯，并能随着现实变化保持语义一致的个人上下文与成长引擎。

识海解决的核心矛盾是：

> 通用 AI 具备推理能力，却不了解一个人的真实历史、现实约束和长期变化；熟悉用户的人了解经历，却未必具备随时可用的专业分析能力。

识海的核心循环是：

```text
生活输入
  -> 原始证据
  -> 情景记忆
  -> 语义候选
  -> 用户裁决或受控自动处理
  -> 规范上下文
  -> 决策与行动
  -> 真实结果
  -> 复盘与修正
```

识海的核心差异化不是“记得更多”，而是：

1. 事实、观点、推断、预测和虚构严格分离。
2. 历史身份与当前状态可以同时存在，不互相覆盖。
3. 用户确认一次变更，所有相关核心视图保持一致。
4. 所有重要结论可以回到证据，所有修正可以回到历史版本。
5. 识灵静默整理，但没有权力把推断伪装成用户事实。
6. 数据、格式和接口不依赖某一家模型或平台。
7. 系统即使停止维护，原始材料和规范语义仍可独立读取与迁移。

产品阶段声明：

> v0.4 面向“通用产品内核、单用户本地优先 MVP”。通用用户压力测试用于避免产品死角，不代表首年建设多租户 SaaS、家庭协作平台或完整数字遗产服务。

---

# 2. 多模型审查建议评分与采纳决策

## 2.1 评分方法

每条建议按五个维度评分，总分 10 分：

| 维度 | 权重 | 说明 |
|---|---:|---|
| 产品价值 | 30% | 是否直接改善用户价值、信任或长期可用性 |
| 基础性 | 25% | 是否属于后期补救成本极高的地基能力 |
| 通用性 | 20% | 是否适用于不同用户，而非单一数据集 |
| 可验证性 | 15% | 是否能够形成明确验收标准 |
| 范围适配 | 10% | 是否属于 PRD 层，且不会让 MVP 失控 |

决策含义：

- **采纳**：进入 PRD v0.4 的正式要求。
- **修改后采纳**：保留目标，修正原建议的错误前提或过度设计。
- **原则采纳、阶段后置**：写入原则或路线图，不进入近期 MVP。
- **转入 SPEC**：需求成立，但算法、字段或选型不在 PRD 锁死。
- **拒绝**：与产品原则冲突、伪精确、位置错误或明显过度工程。

## 2.2 建议决策矩阵

| # | 原子建议 | 分数 | 决策 | 裁决理由 |
|---:|---|---:|---|---|
| 1 | 将一致性拆为强一致、会话一致、最终一致 | 9.9 | 采纳 | 解决“全部立即一致”不可实现的问题，是产品承诺的基础 |
| 2 | 引入有效时间与记录时间的双时态 | 9.9 | 采纳 | 后补成本极高，直接决定历史查询、纠正和审计是否可信 |
| 3 | 将 ChangeSet 升级为一等对象 | 9.8 | 采纳 | 统一识灵、UI、MCP、确认、发布、回滚的唯一写路径 |
| 4 | 所有变更可撤销、可审计、可比较 | 9.7 | 采纳 | 个人核心数据必须允许纠错，信任价值高于实现便利 |
| 5 | 首版定义 Core View 白名单 | 9.5 | 采纳 | 收敛 MVP，先对少量关键视图提供强保证 |
| 6 | 建立最小冲突协议 | 9.7 | 采纳 | “允许矛盾”必须变成可执行流程，不能停留在原则 |
| 7 | 建立完整实体消歧、合并和拆分流程 | 9.4 | 采纳 | 人物误合并是长期记忆中破坏性最大的错误之一 |
| 8 | 使用固定来源等级决定真假 | 6.0 | 修改后采纳 | 来源类型不等于证据强度，改为多维证据向量 |
| 9 | 将可见性、舱室和敏感度设为一等字段 | 9.8 | 采纳 | 权限若不进入数据模型，“工作 Agent 不看健康数据”只能停留在口号 |
| 10 | 建立创伤/高敏信息认知隔离区 | 9.4 | 采纳 | 封存必须同时约束检索、候选生成、摘要和外发 |
| 11 | 定义 Hypothesis 生命周期和反例机制 | 9.3 | 采纳 | 防止 AI 分析堆积和自我强化 |
| 12 | 引入 CoverageWindow 来源覆盖窗口 | 9.6 | 采纳 | 让系统能够诚实区分“没发生”和“没有数据” |
| 13 | 将前瞻记忆建模为 Commitment | 9.2 | 采纳 | 承诺、待办和提醒是从档案走向人生操作系统的关键 |
| 14 | 建立 Review Budget 和候选价值评分 | 9.3 | 采纳 | 没有打扰预算，长期系统必然被审查债务拖垮 |
| 15 | 建立可执行的语义变更测试 Harness | 9.8 | 采纳 | 一致性系统不能只靠问答测试，必须测试变更传播 |
| 16 | 建立失败降级策略 | 9.4 | 采纳 | 解析、传播、索引和模型都可能失败，旧版本必须仍可安全使用 |
| 17 | 视图使用声明式依赖和查询签名 | 8.5 | 转入 SPEC | 产品需要依赖可声明、可追踪，具体语法由 SPEC 决定 |
| 18 | 把事件日志设为唯一真相源 | 7.8 | 修改后采纳 | 需要不可变修订账本，但不能牺牲用户可读、可导出的规范数据 |
| 19 | Markdown 降级为纯视图 | 7.2 | 修改后采纳 | Markdown 可作为规范语义包的人类表示，但不承担事务协调和派生索引职责 |
| 20 | 高置信度低风险事实自动验证 | 6.8 | 修改后采纳 | 只允许确定性机器事实自动确认；个人语义事实仍需候选或明确政策授权 |
| 21 | 全面采用乐观更新 | 7.1 | 部分采纳 | UI 可乐观展示待发布状态，但 Agent 不得把未提交状态当成真实上下文 |
| 22 | 将所有语义压缩为 15-25 个一等对象 | 7.0 | 修改后采纳 | 需要小型稳定内核，但不应为对象数量而对象化；v0.4 收敛为 12 个核心对象 |
| 23 | 所有 Canonical 对象都变成状态机 | 7.3 | 部分采纳 | 仅对有明确生命周期的对象定义状态机，人物等实体不强制套线性状态机 |
| 24 | 决策引擎提前进入 MVP-A | 6.7 | 暂缓 | 决策是核心价值，但必须建立在可信记忆和一致性之上，进入 MVP-C |
| 25 | 建立 Semantic Constitution 语义宪法 | 9.7 | 采纳 | 为所有模型、Agent 和未来实现提供不可越过的产品法律 |
| 26 | 通过 MCP 规定第三方数据标准化 | 5.4 | 拒绝 | MCP 是能力接口，不是摄取协议；应由 Ingestion Contract 负责 |
| 27 | 内置所有高频数据源 ETL | 6.1 | 原则采纳、阶段后置 | 首年只提供少量适配器和导入契约，避免连接器吞噬主线 |
| 28 | 在 PRD 锁定 Neo4j 或事件溯源框架 | 3.0 | 拒绝 | 属于 SPEC/ADR，且会过早锁死架构 |
| 29 | 增加性能与规模目标 | 9.0 | 采纳 | 十年使用必须有容量和延迟约束，但区分 MVP SLO 与长期设计目标 |
| 30 | 增加前端交互与自然语言审查流 | 9.4 | 采纳 | 产品价值最终通过交互交付，不能只设计后端语义 |
| 31 | 支持 Markdown、JSON、可选 JSON-LD 导出 | 9.1 | 采纳 | 开放和可迁移是用户所有权的硬要求，RDF 不进入 MVP 强制项 |
| 32 | 数字遗产进入首年 MVP | 5.8 | 暂缓 | 原则重要，但法律、第三方隐私和授权复杂度过高，进入 Year 2+ |
| 33 | 第三方相关内容继承默认脱敏 | 8.9 | 原则采纳、阶段后置 | 作为长期红线立即写入，完整工作流后置 |
| 34 | 使用小范围影子迁移压测复杂历史数据 | 8.3 | 修改后采纳 | 改为匿名化、合成和授权样本集，避免 PRD 绑定任何私人档案 |
| 35 | 原始材料必须使用非专有格式独立可读 | 9.7 | 采纳 | 这是对抗项目停服、供应商消失和模型淘汰的最强保险 |
| 36 | 多语言、时区和原文/译文并存 | 8.7 | 采纳 | 时间线和跨语言证据长期不可缺，基础字段首版预留，完整体验后置 |
| 37 | 结构化字段之外保留 narrative_context | 9.2 | 采纳 | 人类关系和情绪不能被枚举值压平，必须保留原话和叙事上下文 |
| 38 | 提供 Semantic Diff | 8.8 | 采纳 | “过去怎么看、现在怎么看”是长期上下文的天然高价值能力 |
| 39 | 每个实体拥有独立语义修订号 | 7.7 | 转入 SPEC | 产品要求细粒度修订和依赖失效，具体 revision 粒度由 SPEC 决定 |
| 40 | 全量对账始终整图重算 | 4.5 | 拒绝 | 应采用写后校验、增量对账和低频深度重建的分级策略 |
| 41 | 六种模式做成互斥产品人格 | 5.7 | 拒绝 | 模式应是主动程度、权限和通知策略的组合，不是功能开关迷宫 |
| 42 | 明确个人优先还是通用 SaaS | 9.2 | 采纳 | v0.4 明确：通用内核、单用户本地优先 MVP，首年不做多租户平台 |

## 2.3 整合结论

v0.4 不继续堆功能，而是收敛三件事：

1. **可信变化**：双时态、ChangeSet、冲突、撤销和五态回答。
2. **可控智能**：识灵边界、审查预算、隐私舱室和认知隔离。
3. **可交付 MVP**：白名单视图、单链路 Thin Slice、真实运行的变更测试。

---

# 3. 产品愿景与定位

## 3.1 长期愿景

让每个人拥有一套不依附于单一平台、模型或设备的长期个人上下文基础设施，使过去能够可靠地服务现在和未来。

## 3.2 一句话定位

> 识海是一个由用户拥有、会随着现实变化保持一致，并能帮助用户回忆、理解、决策和成长的个人上下文引擎。

## 3.3 日常价值句

> 用户只需自然地记录或导入生活材料，识灵在后台静默整理；当用户询问过去、处理关系、兑现承诺或做重要决定时，识海提供带证据、带时间、知道自己边界的上下文。

## 3.4 产品类别

- 当前类别：Personal Context & Growth Engine。
- 内部长期愿景：Personal Semantic OS。
- 暂不对外承诺：完整数字孪生、人格复制、命运预测、全自动私人顾问。

## 3.5 差异化定位

| 类别 | 主要能力 | 常见缺口 | 识海的差异 |
|---|---|---|---|
| PKM/Wiki | 文档、双链、组织 | 事实重复、关系静态、依赖人工维护 | 规范语义、动态状态、自动一致性 |
| RAG/搜索 | 从大量材料找相关片段 | 不知道真假、时间、冲突和当前有效性 | 证据、双时态、冲突和覆盖窗口 |
| Agent Memory | 让 Agent 记住用户 | 事实裁决权偏向模型、难迁移 | 用户裁决、可撤销、本地优先 |
| 知识图谱 | 实体和关系 | 缺少用户审查、叙事和成长闭环 | 关系演化、叙事上下文、结果校准 |
| AI 日记 | 记录、摘要、情绪分析 | 容易把分析固化为人格标签 | Hypothesis 生命周期、反例和认知隔离 |

---

# 4. 问题定义

## 4.1 用户问题

1. 个人数据散落在日记、聊天、照片、日历、文件、账单和不同 AI 对话中。
2. 将碎片转为 Markdown 并没有解决人物对齐、时间含义、事实真假和长期一致性。
3. 同一个事实存在多个副本，修改一处后其他页面逐渐失真。
4. 传统知识图谱擅长静态关系，不擅长“曾经是朋友、现在失联、以后重联”的变化。
5. 大模型可以给建议，却容易忽略用户现实限制，也可能把自己的推断重复成事实。
6. 记录越多，检索噪声、审查负担、隐私风险和维护成本越高。
7. 模型、向量库和产品供应商会变化，用户需要能独立带走数据和历史。

## 4.2 产品要解决的核心问题

识海必须同时回答五个问题：

1. **发生了什么？**
2. **系统为什么相信或不相信？**
3. **它在什么时间成立？**
4. **这次变化影响了哪些当前认知和行动？**
5. **用户纠正后，系统是否真的变得更好？**

---

# 5. 目标用户与 Jobs to Be Done

## 5.1 初期目标用户

### Persona A：长期记录者（虚构）

顾澄，31 岁，自由职业者，拥有多年日记、聊天导出、项目文档和照片。他的痛点不是没数据，而是旧记录互相矛盾、人物关系变化后页面不一致、AI 分析不断重复。

### Persona B：低记录、高负荷用户（虚构）

沈宁，42 岁，企业管理者，需要照顾孩子和长辈。她不长期写日记，但有大量日历、照片、医疗单据和聊天。她希望快速回忆承诺和事实，同时严格隔离工作、家庭与健康信息。

### Persona C：隐私敏感用户（虚构）

陆屿，27 岁，频繁更换设备和 AI 工具。他重视本地存储、可迁移、可删除，不希望任何云服务成为自己的唯一记忆入口。

## 5.2 Jobs to Be Done

| 场景 | 用户任务 |
|---|---|
| 记录 | 当生活发生时，我想用最低成本留下线索，而不是先学习数据结构 |
| 回忆 | 当我记不清过去时，我想看到有来源、有时间、不乱补全的答案 |
| 纠正 | 当系统理解错时，我想确认一次就让相关位置全部修正 |
| 关系 | 当关系变化时，我想保留历史，同时看到当前状态和未完成承诺 |
| 决策 | 当我做重大选择时，我想综合历史结果、现实限制、目标和反例 |
| 成长 | 当某个模式反复出现时，我想看到证据和反例，而不是被永久贴标签 |
| 隐私 | 当不同 Agent 使用数据时，我想控制它们能看什么、为什么能看 |
| 迁移 | 当模型、软件或公司消失时，我仍能完整带走并理解自己的数据 |

---

# 6. 产品原则与语义宪法

语义宪法对所有模型、Agent、导入器、视图和未来实现均具有最高优先级。

1. **用户拥有最终裁决权。** AI 可以提出，不得伪装成用户确认。
2. **Source 永远不是自动生成结论的替代品。** 原始材料必须保留来源和定位。
3. **事实不能来源于 View。** 摘要、画像和统计只能派生，不能反向成为证据。
4. **Evidence 高于重复次数。** 一个推断被模型重复一百次，仍然只是同一推断。
5. **Unknown 优于 Hallucination。** 不知道、未覆盖、未确认和有冲突必须明确表达。
6. **Current 不覆盖 Historical。** 当前状态变化必须保留历史有效区间。
7. **Hypothesis 不得自动升级为 Fact。** 必须经过用户确认或符合明确的外部验证规则。
8. **Summary 必须可追溯。** 摘要必须能回到直接来源，禁止无限摘要套摘要。
9. **修正必须传播。** 用户确认的修正必须使所有核心视图和相关提醒失效或更新。
10. **隐私先于便利。** 无权限时系统应少回答，而不是跨舱室猜测。
11. **低摩擦记录先于完美结构化。** 输入可以混乱，结构化负担由系统承担。
12. **人类叙事不可被枚举压平。** 结构化状态之外必须保留原话和叙事上下文。
13. **删除与封存语义必须诚实。** 系统必须说明数据、索引、备份和审计记录分别如何处理。
14. **系统必须能够自证。** 未运行的测试不能被描述为通过。
15. **可迁移性是产品能力，不是退出按钮。** 用户随时可以获得独立可读的数据包。

---

# 7. 产品概念与系统边界

## 7.1 核心概念

| 概念 | 定义 |
|---|---|
| 识海 | 整个个人上下文产品及其长期语义世界 |
| 识灵 | 受权限约束的静默整理者、变更协调者和一致性守护者 |
| Source Vault | 保存原始材料、哈希、原始时间、语言和覆盖范围的源层 |
| Canonical Context | 当前与历史规范语义的用户可迁移表示 |
| Revision Ledger | 记录 ChangeSet、确认、发布、撤销和修订历史的不可变账本 |
| Projection | 人物卡、时间线、当前状态、摘要、报告等派生视图 |
| Context Compiler | 按任务、时间、权限和证据编译有限上下文的机制 |
| MCP | Agent 读取资源和调用受控工具的标准能力接口 |

## 7.2 输入不是知识

```text
多模态材料 != Source 语义 != Episode != Assertion != Verified Context != Self Model
```

- 一段音频首先是 Source。
- 音频中的一次经历可以形成 Episode。
- “我在某日加入某项目”可以形成 reported Assertion。
- “我总是害怕变化”只能成为 Hypothesis 或观点。
- “换工作导致长期焦虑”属于因果解释，必须单独审查。

## 7.3 MCP 不是数据传输总线

- 大文件通过文件系统、对象存储、分享入口或导入器进入 Source Vault。
- MCP 可以发起导入、查询任务、读取结果、提交 ChangeSet。
- 内部后台任务、事件通知和批处理不由 MCP 定义。

---

# 8. 核心语义对象模型

v0.4 将核心对象收敛为 12 类。领域对象可扩展，但不能绕开这些语义边界。

| 对象 | 用途 | 是否规范数据 |
|---|---|---|
| Source | 原始文件、聊天、日记、音视频、日历等来源 | 是，尽量不可变 |
| Entity | 人物、组织、地点、账号、项目、物品 | 是 |
| Episode | 有时间边界的一段经历或事件集合 | 是 |
| Assertion | 可验证、可争议或带视角的陈述 | 是 |
| Relationship | 两个或多个实体间稳定的关系身份 | 是 |
| State | 工作、联系、位置、余额等随时间变化的状态 | 是 |
| Hypothesis | 对模式、因果、人格或未来的可反驳解释 | 是，但与事实隔离 |
| Goal | 用户想达到的目标和衡量标准 | 是 |
| Commitment | 承诺、待办、责任、提醒和截止日期 | 是 |
| Decision | 问题、选项、约束、假设、预测和选择 | 是 |
| Outcome | 行动结果、副作用、校准和复盘 | 是 |
| ChangeSet | 对规范语义的唯一受控变更入口 | 是，进入修订账本 |

派生而非规范对象：Projection、Embedding、搜索缓存、统计、生成摘要、推荐排序。

## 8.1 Assertion 类型

| 类型 | 含义 |
|---|---|
| observed | 有直接观察或确定性系统记录 |
| reported | 用户或第三方陈述 |
| quoted | 引用或粘贴的外部内容 |
| opinion | 某个主体的主观看法 |
| inferred | 由模型或规则推断 |
| analysis | 人工或 AI 分析 |
| predicted | 预测、计划、情景推演 |
| fictional | 创作、角色、假设案例 |
| disputed | 存在未解决冲突 |
| unknown | 无法安全判断 |

---

# 9. 时间、证据与认知状态

## 9.1 双时态模型

每个重要 Assertion、Relationship 和 State 必须区分：

- `valid_from / valid_to`：现实中何时成立。
- `recorded_at`：识海何时得知或写入。
- `source_created_at`：来源本身何时产生。
- `ingested_at`：来源何时进入系统。

例：用户在 2030 年补录“2028 年已经离开某项目”。现实有效时间是 2028 年，系统记录时间是 2030 年，两者不能混用。

## 9.2 覆盖窗口 CoverageWindow

每个来源系统记录：

- 覆盖起止时间。
- 是否连续。
- 缺口区间。
- 数据是否完整导出。
- 来源时区和语言。

当查询落在覆盖范围外时，回答必须提示“该时间段没有数据覆盖”，不得推断事件未发生。

## 9.3 多维证据模型

不采用简单 E0-E4 来源排行榜。证据至少记录：

| 维度 | 示例 |
|---|---|
| proximity | 直接观察、本人陈述、第三方转述、多步推断 |
| integrity | 原文件、哈希、元数据、截图、无原件 |
| corroboration | 独立来源数量及是否真正独立 |
| perspective | 用户、第三方、系统、机构 |
| inference_distance | 原句、一步推断、多步因果推断 |
| review_status | 未审查、用户确认、被否认、存在争议 |
| freshness | 当前状态距最后有效证据的时间 |

`evidence_score` 可以动态计算，但不能替代上述原始维度，也不能单独决定事实真伪。

## 9.4 五态回答协议

所有事实型回答必须标识以下状态之一：

1. `verified`：已有确认或强直接证据。
2. `unconfirmed`：存在候选，但尚未裁决。
3. `disputed`：来源互相冲突。
4. `not_covered`：对应时间或领域没有足够数据覆盖。
5. `stale`：只有已过期快照，无法代表当前状态。

必要时补充 `unknown`，表示有覆盖但仍无法判断。

---

# 10. 反应式一致性系统

## 10.1 产品承诺

用户确认一次规范变更后，系统必须知道哪些核心视图、提醒、缓存和派生结论受到影响，并保证它们更新或显式失效。

## 10.2 一致性分级

| 等级 | 范围 | 产品保证 |
|---|---|---|
| L1 规范一致 | Assertion、Relationship、State、Commitment、Decision | ChangeSet 原子成功或全失败，不发布半完成规范状态 |
| L2 会话一致 | 人物卡、当前状态、关系时间线、开放承诺、关键提醒 | 发布后同一会话能够读取新版本；未更新时不得返回旧值冒充最新 |
| L3 最终一致 | 长摘要、统计、画像、趋势、冷分析、Embedding | 旧版本立即标记 stale，后台增量重建 |

外部 Agent 读取 L2/L3 视图时，必须获得 `data_revision`、`view_revision` 和 `freshness_status`。当任务涉及不可逆外部行动时，不得使用 stale 视图。

## 10.3 Core View 白名单

MVP 首先保证以下视图：

1. 人物卡。
2. 当前状态。
3. 关系时间线。
4. 开放 Commitment。
5. 提醒列表。
6. 检索答案引用缓存。

白名单外视图允许异步生成，但必须可失效、可重建、可解释依赖。

## 10.4 依赖传播原则

- 依赖必须声明，不能由 LLM 每次临时猜测。
- LLM 可以提出“可能还有这些影响”，但不能控制核心传播规则。
- 核心谓词使用明确影响规则。
- 动态查询视图需要可追踪的查询签名，具体格式进入 SPEC。
- 每次发布后生成影响回执。

## 10.5 对账分级

- 写后校验：检查 L1 和 L2 revision 是否一致。
- 日常增量对账：失败队列、stale 视图、孤儿引用、未消费 ChangeSet。
- 周期深度对账：从规范数据重建核心投影并比较结果。
- 重大升级对账：模型、Schema、索引或迁移版本变更后执行完整回归。

---

# 11. ChangeSet 变更协议

## 11.1 定义

ChangeSet 是任何规范写入的唯一入口。用户、识灵、外部 Agent、导入器和迁移程序都不能绕过 ChangeSet 直接修改规范状态。

## 11.2 最小产品字段

| 字段 | 说明 |
|---|---|
| changeset_id | 唯一标识 |
| base_revision | 提案基于哪个规范版本 |
| actor | 用户、识灵、外部 Agent 或导入器 |
| trigger_sources | 触发提案的 Source 与证据定位 |
| proposals | 新增、修正、结束、合并、拆分、封存或删除提案 |
| impact_set | 受影响对象、Core View、提醒与缓存 |
| risk_level | low、medium、high、critical |
| confirmation_policy | 自动、事后可撤销、单次确认、二次确认 |
| status | proposed、reviewing、approved、rejected、publishing、published、failed、reverted |
| published_revision | 成功发布后的规范版本 |
| receipt | 成功、等待重建、失败和跳过项 |
| rollback_reference | 撤销或恢复所需引用 |

## 11.3 ChangeSet 生命周期

```text
proposed
  -> reviewing
  -> approved | rejected
  -> publishing
  -> published | failed
  -> reverted（可选）
```

## 11.4 自动处理边界

可自动发布：哈希、文件类型、字节数、导入时间等确定性机器事实，以及用户预先授权的无语义机械修复。

默认不可自动验证：关系性质、信任、亲密度、债务、健康诊断、人格模式、因果关系、重大目标和第三方隐私内容。

低风险高置信候选可以采用“事后可撤销”策略，但必须保持明显标识、可在摘要中回顾，且不能用于高风险 Agent 行动。

---

# 12. 冲突、纠正与撤销

## 12.1 冲突协议

1. **检测**：同一主体、相同或互斥谓词、有效时间重叠、值不兼容。
2. **分类**：时间演化、来源冲突、视角差异、实体误合并、单位/时区错误、语义歧义。
3. **呈现**：并列展示证据、来源、时间和覆盖缺口。
4. **处置**：保留双版本、关闭旧有效区间、请求用户裁决、修正实体、标记 disputed。
5. **传播**：冲突未解决前，依赖视图显示冲突，不自行挑选漂亮答案。

## 12.2 最小规则

- 用户明确确认可以提升 review status，但不能抹去反证。
- 同一来源的新记录不必然覆盖旧记录，先判断是状态变化还是纠错。
- 不同来源冲突默认进入 disputed。
- “没有聊天记录”不能自动证明“已断联”。
- 新证据可以结束旧状态的有效区间，但历史记录继续保留。

## 12.3 纠正与撤销

- 用户可撤销整个 ChangeSet。
- 高级流程可撤销单个 proposal，但系统必须重新计算其影响。
- 撤销通过新的修订记录表达，不静默擦除历史。
- 视图、提醒和检索缓存必须随撤销恢复或重建。
- 用户可以查看两个时间点之间的 Semantic Diff。

## 12.4 删除、封存与遗忘

| 操作 | 默认语义 |
|---|---|
| 降权 | 降低默认检索活跃度，不改变真值 |
| 归档 | 从默认工作集移出，仍可搜索 |
| 封存 | 默认不检索、不摘要、不生成候选，需主动解封 |
| 软删除 | 从产品视图和索引移除，保留有限撤销窗口 |
| 硬删除 | 按明确策略处理原始数据、索引、缓存和备份；审计仅保留不含正文的最小删除证明 |

“删除只能新增删除事件、数据永不真正消失”的建议不被采纳为绝对原则，因为它与用户删除权冲突。

---

# 13. 实体消歧与关系演化

## 13.1 实体消歧流程

```text
标准化
  -> 候选分块
  -> 多信号相似度
  -> 合并建议
  -> 用户确认
  -> 合并发布
  -> 可拆分回滚
```

相似度信号可包括：名称、别名、账号、共同联系人、来源上下文、时间重叠、地点和用户确认。具体权重与阈值进入 SPEC。

人物实体默认不自动合并。确定性账号映射可按用户预授权自动关联，但仍需保留原始 Source identity。

## 13.2 关系模型

关系至少区分：

- `origin`：同学、亲属、同事、网络认识等历史来源。
- `role`：朋友、伴侣、合作伙伴、债权人等可并存角色。
- `contact_state`：active、low_frequency、no_contact、blocked、unknown。
- `closeness`：亲密程度，允许未知和区间表达。
- `trust`：信任判断，必须带 perspective 和证据。
- `sentiment`：主体当时的主观感受。
- `valid_interval`：状态有效区间。
- `narrative_context`：保留无法被枚举表达的原话与背景。

## 13.3 关系变更规则

| 输入 | 可生成的候选 | 禁止自动改变 |
|---|---|---|
| “我们不再联系” | contact_state=no_contact | origin、trust、closeness、冲突原因 |
| 长期没有消息 | low_frequency 建议或覆盖缺口提示 | no_contact |
| 一次争吵 | Episode、sentiment 观点 | 永久 trust、人格标签 |
| 发生借款 | Obligation/Commitment | 好友身份、亲密度 |
| 重新联系 | contact_state=active 或 reconnected Episode | 自动恢复历史亲密度 |

## 13.4 关系事件、状态和判断分离

- 关系事件：某次争吵、借款、重联、共同旅行。
- 关系状态：某段时间的 contact_state、role。
- 关系判断：“我觉得对方不信任我”，属于 viewpoint 或 Hypothesis。

---

# 14. 识灵设计

## 14.1 定位

识灵是一个长期运行、权限受限、可审计的 Personal Context Curator。它不是用户的主人，不拥有真相，也不应被实现为七个互相辩论的 Agent。

> 七种职责，一个协调内核，多个可替换能力模块。

## 14.2 七类职责

| 职责 | 产品行为 |
|---|---|
| Observer | 发现新增来源、遗漏字段、覆盖变化和待处理失败 |
| Interpreter | 提取实体、Episode、Assertion、Commitment 和候选关系 |
| Integrator | 去重、别名对齐、聚类、来源连接和时间标准化 |
| Impact Analyst | 依据显式规则计算 ChangeSet 影响范围 |
| Curator | 合并候选、安排审查、控制摘要与记忆层级 |
| Guardian | 权限、封存、敏感识别、外发预览和提示注入防护 |
| Reconciler | 写后校验、增量对账、失效检测和评测调度 |

## 14.3 系统诚实性

识灵必须能够明确说：

- “我已更新，并验证了哪些视图。”
- “我只生成了候选，尚未改变事实。”
- “我不确定，需要你确认。”
- “对应时间没有数据覆盖。”
- “传播失败，系统仍在使用哪个安全版本。”
- “我没有权限访问该舱室。”

识灵不得使用陪伴话术掩盖证据缺失、权限拒绝或系统失败。

## 14.4 提示注入边界

日记、网页、聊天、文件和转写内容均视为不可信数据。Source 中出现的“忽略规则”“调用工具”“上传文件”等文字不能成为系统指令。

---

# 15. 审查预算与低打扰机制

## 15.1 目标

识灵不能把用户的生活转化为另一份全职维护工作。

## 15.2 候选价值评分

候选价值由以下因素决定：

- 对当前目标、承诺和决策的影响。
- 变更影响范围。
- 风险和敏感度。
- 时效性。
- 不确定性。
- 是否重复出现。
- 用户对同类候选的历史接受、拒绝和忽略行为。

具体算法进入 SPEC，评分不得成为事实置信度。

## 15.3 审查策略

- Critical：安全、删除、人物误合并等立即提示。
- High：重大关系、工作、债务、健康等进入优先审查。
- Normal：进入每日或每周摘要。
- Low：静默保留、聚合或衰减，不强制处理。

用户可以确认、修改、拒绝、稍后、批量处理、永不再问。

## 15.4 审查预算默认目标

- 新用户每次不超过 3 个高价值问题。
- 稳定使用后每周审查中位时间目标不超过 5 分钟。
- 当未处理高价值候选超过预算时，停止生成低价值语义候选。
- 被连续拒绝的提议类型自动降低频率，但不能因此改变事实判断。

---

# 16. 记忆生命周期与长期规模

## 16.1 脑启发而非脑复制

| 人类记忆类比 | 识海机制 |
|---|---|
| 感觉缓冲 | Capture Inbox，允许低结构、快速输入 |
| 海马式情景记忆 | Episode Store，快速保存具体经历和差异 |
| 慢速语义巩固 | 从多个 Episode 提出稳定 Assertion 或 Hypothesis |
| 工作记忆 | 按任务编译的小型上下文 |
| 遗忘 | 降低提取活跃度、归档或封存，而非静默改写真相 |
| 再巩固 | 新修订 supersede 旧解释，保留证据与历史 |

## 16.2 多分辨率记忆

```text
原始材料
  -> 可定位片段
  -> Episode
  -> 日/周/项目阶段摘要
  -> 长期 Assertion 与关系状态
  -> Hypothesis 与决策校准
```

每一级摘要必须能回到更低一级证据。底层变化后，上层摘要必须失效。

## 16.3 热、温、冷与封存

| 层级 | 内容 | 默认策略 |
|---|---|---|
| Hot | 当前目标、开放承诺、活跃决策、核心当前状态 | 高频检索，严格新鲜度 |
| Warm | 历史 Episode、关系阶段、已关闭项目 | 问题相关时展开 |
| Cold | 大规模聊天、媒体、旧附件和低频来源 | 按需召回，成本受控 |
| Sealed | 用户封存的创伤、高敏或限制内容 | 默认不检索、不摘要、不生成候选 |

## 16.4 三个独立维度

- `truth_confidence`：证据支持程度。
- `retrieval_activation`：当前任务下值得被想起的程度。
- `retention_policy`：允许保存、访问和删除的规则。

三者禁止混用。频繁访问不能提高真值，时间衰减不能自动让 Verified 事实变假。

## 16.5 长期数据策略

- 海量原始流不采用“一条消息一个 Markdown”。
- 大型媒体使用内容哈希和清单，避免重复。
- 原始数据优先使用公开、可长期读取的格式。
- Embedding、向量、图索引和缓存必须可重建。
- 默认只对高价值内容生成昂贵的语义索引。
- 检索按权限、时间、关键词/语义、图关系、证据质量和反例逐级收敛。

---

# 17. 隐私、权限与第三方数据

## 17.1 多主体

每条语义记录需要区分：

- `owner`：谁拥有这份识海。
- `subject`：记录描述的是谁。
- `recorder`：谁记录或提供了内容。
- `viewer/caller`：当前请求者或 Agent。

## 17.2 上下文舱室

默认域包括：personal、work、family、health、finance、legal、relationship、creative。

舱室不是文件夹，而是访问控制和上下文编译的策略边界。一条记录可以属于多个域，但跨域读取必须满足所有适用策略。

## 17.3 敏感度与可见性

| 等级 | 默认行为 |
|---|---|
| normal | 本地授权 Agent 可读取 |
| private | 仅用户和明确授权模式读取 |
| restricted | 字段级裁剪、按目的临时授权 |
| sealed | 默认完全排除，需用户主动解封 |

## 17.4 权限原则

- 默认最小披露。
- Agent 权限按身份、目的、舱室、字段、时间和动作综合判断。
- 临时授权必须有范围和自动到期时间。
- 外发前提供脱敏预览。
- 敏感识别可以自动建议，但不能自动扩大访问范围。
- 工作 Agent 不得通过其他关系或摘要旁路推断受限健康信息。

## 17.5 第三方隐私

- 用户可以记录与自身经历相关的第三方信息，但系统应最小化不必要细节。
- 分享和导出时，第三方敏感内容默认脱敏。
- 数字遗产场景中，第三方相关正文默认不继承，除非存在明确规则和合法授权。
- 未成年人、医疗、亲密关系和第三方财务采用更高默认保护。

---

# 18. 核心用户体验

## 18.1 首页

首页优先显示：

1. 当前重要状态和开放承诺。
2. 识灵最近完成的静默工作。
3. 不超过审查预算的高价值确认。
4. 数据覆盖、同步和视图新鲜度。
5. 快速记录和自然语言查询入口。

首页不展示大型功能介绍，不用技术术语要求普通用户理解 ChangeSet、Projection 或 Revision。

## 18.2 快速记录

- 文本、语音、图片、视频、文件和分享入口。
- 记录操作目标小于 10 秒。
- 用户不必先选择对象类型和关系枚举。
- 完成后提示“已保存原始记录”，语义整理可以后台进行。

## 18.3 自然语言审查

面向用户的文案示例：

> 你刚才提到“最近不再和顾言联系”。我可以把当前联系状态改为“已停止联系”，历史上的“大学同学”关系会保留。这样会更新人物卡、关系时间线，并暂停一条联系提醒。要更新吗？

高级用户可展开查看证据、proposal、影响图和修订信息。

## 18.4 影响预览

确认前显示：

- 将新增或结束哪些状态。
- 哪些信息不会被改变。
- 影响的 Core View 和提醒。
- 是否涉及敏感内容。
- 是否可撤销。

## 18.5 查询与回答

回答默认包含：

- 简明结论。
- 认知状态。
- 有效时间。
- 主要证据。
- 冲突和覆盖盲区。
- “查看原始来源”和“纠正”入口。

## 18.6 Semantic Diff

用户可以比较两个时间点：

- 当前状态差异。
- 关系角色与联系状态差异。
- 用户观点或 Hypothesis 的变化。
- 哪些变化来自新事实，哪些来自后来补录或纠正。

## 18.7 决策室

Decision 至少包含：问题、选项、约束、假设、支持/反对证据、选择、预测、复盘日期和结果。

系统必须区分“最优方案”和“用户现实可执行方案”，不以抽象最优替代实际生活。

---

# 19. MCP、Agent 与输入层边界

## 19.1 MCP Resources

- 当前状态与新鲜度。
- 人物与关系时间线。
- Episode、Assertion、证据和覆盖窗口。
- Goal、Commitment、Decision 和 Outcome。
- 冲突、待确认项和系统健康状态。

## 19.2 MCP Tools

| 权限级别 | 示例 |
|---|---|
| read | search_context、get_evidence、get_timeline、get_relationship |
| propose | propose_changeset、propose_entity_merge、open_decision |
| append | record_source、record_diary、record_outcome |
| controlled mutate | approve_changeset、correct_assertion、revert_changeset |
| destructive | seal_item、delete_item、export_sensitive_pack |

所有工具响应包含：权限判定、数据 revision、新鲜度、证据或 missing_evidence、变更回执。

## 19.3 外部 Agent 规则

- Agent 不能直接 Verify 个人语义事实。
- stale 视图不能驱动不可逆外部操作。
- 无权限或证据不足时必须拒绝或降级。
- Agent 不接收不必要的完整档案，只获取任务所需最小上下文。

## 19.4 Ingestion Contract

输入适配器至少提供：原始资源引用、来源系统、原始时间、时区、语言、内容哈希、解析产物和解析器版本。

第三方可以提供 OCR、ASR、视频切片和 Markdown 转换；识海不把这些能力作为核心护城河。

---

# 20. 功能需求

## 20.1 P0：可信变化核心

- FR-001：接收文本与第三方规范输入包。
- FR-002：保存 Source 及其证据定位、哈希和覆盖窗口。
- FR-003：识灵生成 Entity、Assertion、RelationshipState 候选。
- FR-004：所有规范写入通过 ChangeSet。
- FR-005：提供自然语言审查和高级影响预览。
- FR-006：发布后更新或失效 Core View。
- FR-007：提供 ChangeSet 回执、历史和撤销。
- FR-008：回答使用五态认知协议。
- FR-009：支持双时态历史查询。
- FR-010：提供最小冲突检测与并列呈现。
- FR-011：提供实体合并候选和拆分回滚。
- FR-012：权限和舱室在查询层强制执行。

## 20.2 P1：静默整理与前瞻记忆

- FR-101：候选聚合、去重和价值排序。
- FR-102：Review Budget 和分级通知。
- FR-103：Episode 聚类与分层摘要。
- FR-104：Commitment 提取、状态和提醒。
- FR-105：增量对账、失败队列和 stale 检测。
- FR-106：Semantic Diff。
- FR-107：低风险机械变更的事后可撤销策略。
- FR-108：多语言原文与翻译对照。

## 20.3 P2：成长与决策

- FR-201：Hypothesis 的支持证据、反例、范围和生命周期。
- FR-202：Decision、Outcome 和 Calibration 闭环。
- FR-203：周/月/年度复盘。
- FR-204：基准、乐观、悲观情景推演。
- FR-205：跨阶段行为和结果比较。
- FR-206：可执行性约束和行动跟进。

## 20.4 P3：生态与长期能力

- FR-301：加密多设备同步。
- FR-302：有限高价值连接器。
- FR-303：Context Pack 导出与导入。
- FR-304：专业 Agent 权限模板。
- FR-305：家庭授权和数字遗产原则的完整工作流。
- FR-306：A2A 或其他 Agent 互操作适配。

---

# 21. 非功能需求与服务等级

## 21.1 可靠性

- Source 导入不得静默丢失。
- L1 规范变更必须原子提交或完全失败。
- 失败时旧安全版本保持可读。
- 所有自动操作有 actor、时间、版本和结果。
- Derived Index 可从规范数据重建。

## 21.2 MVP 服务等级目标

| 项目 | MVP 目标 |
|---|---|
| 快速记录确认 | 本地交互即时完成，不等待语义处理 |
| 常规规范查询 | 本地 p95 小于 2 秒 |
| ChangeSet 发布 | 常规本地场景 p95 小于 3 秒 |
| Core View 会话一致 | 发布后 5 秒内可读取新 revision |
| L3 旧视图失效标记 | 发布后立即可见 |
| 冷来源搜索 | p95 小于 10 秒，允许展示渐进结果 |
| 撤销可用性 | 已发布 ChangeSet 在策略允许期内一键撤销 |

## 21.3 长期设计容量目标

以下是架构设计目标，不是 Micro-MVP 验收门槛：

- 单用户百万级 Assertion/State。
- 千万级 Source 片段。
- 1 TB 级原始媒体和附件。
- 十年以上时间跨度。
- 深度对账采用增量/分区处理，不要求每次整图重算。

## 21.4 可移植性

- 必须导出原始文件及校验清单。
- 必须导出人类可读 Markdown 与结构化 JSON。
- 未知扩展字段往返时不得丢失。
- 可选导出 JSON-LD；RDF 不作为 MVP 强制依赖。
- 软件停止运行后，Source Vault 和 Canonical Context 仍可独立读取。

## 21.5 可访问性与国际化

- 支持键盘操作、屏幕阅读和高对比模式。
- 时间内部保存 UTC、原始时区和偏移信息。
- 原文和翻译分离，翻译不能覆盖证据原文。
- 界面不以技术术语作为普通用户完成任务的前提。

---

# 22. 评测、自证与模型升级

## 22.1 测试状态

系统必须分别记录：

- `suite_defined`
- `suite_executed`
- `suite_passed`

未执行不得称为通过，执行失败不得只展示“已建立测试集”。

## 22.2 Semantic Change Harness

评测由初始规范状态、输入 Source、ChangeSet、预期规范状态、预期影响视图和禁止变化组成。

核心变更测试至少覆盖：

1. 历史关系保留、当前联系结束。
2. 工作入职与离职。
3. 住址和位置变化。
4. Obligation 结清。
5. 人物合并与拆分。
6. Snapshot 过期。
7. Commitment 完成、取消与失效。
8. Restricted 字段权限过滤。
9. Hypothesis 被反例削弱。
10. 覆盖窗口外查询。
11. 冲突来源并列。
12. ChangeSet 撤销后视图恢复。

## 22.3 黄金问答集

覆盖当前状态、人物关系、事件时间线、证据、冲突、观点变化、任务、决策、权限和未知回答。

## 22.4 模型升级门禁

- 记录模型、Prompt、Embedding、Reranker 和解析器版本。
- 核心测试不回归才允许设为默认。
- 升级产物先进入隔离评测，不直接重写 Verified Context。
- 支持回滚到上一个模型和索引版本。
- 模型升级不得改变 Source 和用户确认历史。

---

# 23. 指标体系

## 23.1 北极星指标

> 每季度完成并复盘的高质量决策闭环数量。

高质量决策闭环必须具备：明确问题、至少两个选项、至少一个现实约束、显式假设、最终选择、结果记录和复盘结论。

## 23.2 早期存活指标

- 周内成功记录天数。
- 首次记录到首次有证据回忆的时间。
- 捕获完成率和解析成功率。
- 每周审查时间。
- 用户跳过、拒绝和批量处理比例。

这些指标用于发现使用负担，不作为制造上瘾的目标。

## 23.3 信任与质量指标

- 事实回答引用覆盖率。
- 引用精确率。
- 双时态正确率。
- 冲突检出率和错误冲突率。
- 实体错误合并率。
- 用户纠正后的传播成功率。
- stale 视图误读率。
- 权限泄漏事件数。
- “无覆盖”正确表达率。

## 23.4 识灵指标

- 错误打扰率：被用户标记为不应提出的高优先候选占比。
- 重复提问率：相同语义问题在有效期内被再次提出的占比。
- 候选接受率：按类型观察，不追求越高越好。
- 静默处理成功率。
- 传播失败恢复时间。
- Review Debt 年龄分布。

## 23.5 价值指标

- 回忆任务一次解决率。
- Commitment 按期闭环率。
- 决策复盘完成率。
- 建议执行率。
- 预测校准度。
- 用户纠正后同类错误下降幅度。

---

# 24. MVP 与路线图

## 24.1 Micro-MVP：一条窄而硬的链路

目标：证明“事实变化可以安全地全局生效”。

范围：

1. 输入一段虚构日记。
2. 识灵提出一个人物联系状态 ChangeSet。
3. 用户确认一次。
4. RelationshipState 原子更新。
5. 人物卡与关系时间线读取新 revision。
6. 历史关系、信任和人格判断不被错误修改。
7. 用户可撤销并看到视图恢复。

不做：通用依赖语言、全量聊天迁移、多 Agent、决策引擎、图数据库平台。

## 24.2 MVP-A：可信变化核心（0-3 个月）

- Source、Entity、Assertion、Relationship、State、ChangeSet。
- 双时态、CoverageWindow、五态回答。
- 三个 Core View：人物卡、关系时间线、当前状态。
- 自然语言审查、影响预览、回执和撤销。
- 最小冲突协议、实体合并候选、可见性字段。
- 12 个可执行语义变更测试。

## 24.3 MVP-B：识灵静默整合（3-6 个月）

- Episode、Commitment、Review Budget。
- 候选聚合、价值评分、低打扰摘要。
- 增量对账、失败队列、Core View 白名单扩展。
- 匿名化/合成复杂数据影子迁移，压测消歧与传播。
- Semantic Diff。

## 24.4 MVP-C：决策与成长（6-12 个月）

- Goal、Decision、Outcome、Hypothesis。
- 反例、有效范围和 Hypothesis 生命周期。
- 决策室、复盘、校准和阶段比较。
- 本地加密备份和 Markdown+JSON Context Pack。

## 24.5 Year 2

- 多设备加密同步。
- 2-3 个高价值连接器。
- 工作、家庭、健康等权限模板。
- 专业 Agent 接入和模型评测平台。
- 数字遗产与第三方隐私的法律/伦理验证，不承诺立即上线。

## 24.6 Year 3-5

- Context Pack 生态与开放扩展。
- 多模态长期档案和离线模型。
- 家庭授权、托管人和数字遗产工作流。
- A2A 或其他 Agent 协作协议适配。
- 长期决策校准和跨阶段自我模型。

## 24.7 首年明确非目标

- 多租户 SaaS 平台。
- 完整家庭协作。
- 数字遗产上线。
- A2A 和复杂多 Agent 编排。
- 支持所有聊天、相册和可穿戴设备连接器。
- 自动人格诊断。
- 全自动因果推断。
- 强制采用 Neo4j、RDF 或某个事件溯源框架。

---

# 25. 风险、降级与非目标

## 25.1 主要风险

| 风险 | 表现 | 缓解 |
|---|---|---|
| 过度工程 | 长期停留在架构期 | Micro-MVP、Core View 白名单、首年非目标 |
| 审查疲劳 | 候选积压、用户弃用 | Review Budget、合并、节流、低价值停产 |
| 自我强化 | AI 假设重复成事实 | 认知隔离、反例、禁止自动升格 |
| 本体僵化 | 枚举压平复杂人生 | narrative_context、unknown、可扩展字段 |
| 错误合并 | 两个人被当成同一人 | 人物合并确认、拆分回滚 |
| 旧视图误导 | Agent 使用过期上下文 | revision、新鲜度、stale 禁止高风险行动 |
| 隐私泄漏 | 跨舱室、外发、旁路推断 | 最小披露、强制过滤、脱敏预览、审计 |
| 停服锁定 | 软件或公司消失 | 开放格式、独立可读 Source、可重建派生层 |
| 数据规模 | 全量索引和重建变慢 | 分层记忆、增量传播、按需索引、分区对账 |
| 心理伤害 | 创伤被主动唤起、永久标签 | sealed、用户控制、模式边界、不做诊断 |

## 25.2 错误降级

- 解析失败：保留 Source，标记待处理，不生成猜测。
- 实体不确定：创建 provisional candidate，不自动合并。
- Canonical 发布失败：不增加 revision，旧版本继续可用。
- L2 视图失败：显示更新中或直接从 Canonical 读取，不返回旧值冒充最新。
- L3 视图失败：保留旧版本并标记 stale。
- 向量索引失败：退化为全文、时间和明确关系查询。
- 模型不可用：继续支持记录、浏览、手动纠正、导出和规则检索。
- 对账发现问题：先隔离、生成报告；除已授权机械修复外不静默改写语义。

## 25.3 永久非目标

- 宣称复制完整人格或意识。
- 代替医生、心理医生、律师或财务顾问。
- 根据有限记录预测命运。
- 将用户停留时长作为最高目标。
- 将第三方隐私视为用户可以无限授权的资产。
- 通过锁定数据制造切换成本。

---

# 26. 虚构端到端验收案例

以下均为合成案例，不对应任何真实个人。

## Case A：历史同学与当前断联

初始状态：顾澄与周桥的 `origin=university_classmate`，当前 `contact_state=active`。

新输入：“我和周桥去年秋天后就没联系了，现在也不打算主动联系。”

预期：

- 提出 contact_state=no_contact，valid_from 为模糊时间候选。
- origin 保持 university_classmate。
- 不自动修改 trust、closeness 或对方人格。
- 影响人物卡、关系时间线和一条联系提醒。
- 用户确认后 Core View 同会话一致。
- 撤销后恢复原 revision。

## Case B：工作状态事后补录

2030 年用户补录：“我在 2029 年 4 月离开了澄光工作室。”

预期：

- valid_to=2029-04，recorded_at=2030 当前时间。
- 系统可以分别回答“现实中何时离开”和“识海何时知道”。
- 当前工作视图不再显示旧单位。
- 2029 年 3 月的历史查询仍显示在职。

## Case C：承诺与关系变化

用户记录：“答应下周归还许闻借给我的相机。”随后记录“我们暂时不联系了”。

预期：

- 归还相机形成 Commitment，不因 no_contact 自动取消。
- 影响分析提示仍有未完成承诺。
- 用户可以保留、改期或取消，取消需要原因和状态记录。

## Case D：健康舱室与工作 Agent

用户上传一份虚构体检记录，并询问工作 Agent：“我下周适合安排多少会议？”

预期：

- 工作 Agent 无权读取健康正文。
- 若用户未授权，只能根据工作日历回答，并说明未使用健康信息。
- 临时授权后只提供完成任务所需的最小结论，不暴露完整报告。

## Case E：来源冲突

日记说“项目已经结束”，日历显示下周仍有项目会议，聊天中对方说“只是暂停”。

预期：

- 生成 disputed 状态并并列证据。
- 不自动选择“结束”或“暂停”。
- 项目视图显示存在冲突。
- 用户裁决后保留冲突历史与裁决依据。

## Case F：无覆盖窗口

用户问：“2025 年 2 月我有没有和某人联系？”聊天导出只覆盖 2025 年 6 月之后。

预期：

- 回答 not_covered。
- 不以“没有搜索结果”断言“没有联系”。
- 显示来源覆盖开始时间。

## Case G：Hypothesis 被反例削弱

识灵提出：“用户可能在遇到冲突时倾向回避。”随后出现多次主动解决冲突的 Episode。

预期：

- Hypothesis 保持与事实隔离。
- 新反例进入 evidence_against。
- 状态从 active 转为 challenged 或 weakened。
- 不删除历史，也不继续在首页用确定性文案展示。

---

# 27. 开放问题与后续 SPEC

## 27.1 仍需产品决策

1. 英文名 Noetide 的商标、域名和应用商店可用性。
2. 用户预授权自动处理的最大范围。
3. 默认 Review Budget 是否因用户模式而变化。
4. Sealed 内容的紧急访问和恢复策略。
5. 第三方数据的分享、继承和纠纷处理原则。
6. 个人工具与未来商业产品的开源边界。
7. 哪些 Decision 类型需要专业风险提示。

## 27.2 后续文档顺序

PRD v0.4 批准后，依次编写：

1. `Semantic Object Model SPEC`
2. `Bitemporal & Evidence SPEC`
3. `ChangeSet & Consistency SPEC`
4. `Shiling Policy SPEC`
5. `Privacy & Access Policy SPEC`
6. `MCP Contract SPEC`
7. `Storage, Index & Portability SPEC`
8. `Semantic Test Harness SPEC`
9. `Migration & Import Contract SPEC`

## 27.3 开工门禁

只有以下事项确认后才进入实现：

- Micro-MVP 范围不再扩张。
- L1/L2/L3 一致性边界获得确认。
- 12 个核心对象和五态回答协议获得确认。
- 识灵自动权限边界获得确认。
- 三个 Core View 与合成验收案例获得确认。
- 首年非目标获得确认。

---

# 结语

识海的长期价值不在于让 AI 保存尽可能多的文本，而在于让一个人的长期上下文能够：

- 被保存，但不被平台绑架；
- 被理解，但不被模型擅自定义；
- 被更新，但不覆盖历史；
- 被遗忘或封存，但不产生虚假的删除承诺；
- 被不同 Agent 使用，但始终服从用户权限；
- 被现实结果不断校准，而不是在 AI 自我重复中变得越来越自信。

> 识海保存的是可追溯的人生上下文；识灵维护的是变化与一致性；用户始终拥有事实、权限和最终解释权。
